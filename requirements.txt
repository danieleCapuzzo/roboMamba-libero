# torch, causal-conv1d, mamba-ssm are NOT here.
# Install them yourself first (CUDA-version specific), see pyproject.toml or readme.md.

# Backbone: tokenizer + CLIP vision encoder + Mamba LLM wrapper
transformers==4.40.1
tokenizers==0.19.1
sentencepiece==0.1.99
timm==0.9.10

# LoRA fine-tuning
peft==0.11.1

# trunk construction and device/dtype placement
accelerate==0.30.1

# Checkpoint IO
safetensors==0.8.0
huggingface_hub==0.36.2

# LIBERO HDF5 data pipeline
h5py==3.16.0
opencv-python==4.6.0.66
numpy==1.26.4

# Training loop
tqdm==4.70.0
